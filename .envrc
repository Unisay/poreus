use flake
source_env_if_exists .envrc.local

# `cachix` CLI looks for CACHIX_AUTH_TOKEN; bridge from CACHIX_KEY if set in
# .envrc.local so `cachix push poreus ./result` works without renaming.
export CACHIX_AUTH_TOKEN="${CACHIX_AUTH_TOKEN:-${CACHIX_KEY:-}}"
